# Dipendenze Python pinned alle versioni effettivamente testate
# (output di `pip freeze` da un setup di riferimento Debian 13 trixie,
# Python 3.11.9, su Raspberry Pi 4).
#
# Per aggiornare le versioni: dopo aver verificato compatibilita',
# lanciare `pip install -U <pacchetti>` e rigenerare questo file con
# `pip freeze > requirements.txt`.

# Framework HTTP e ASGI
fastapi==0.135.3
starlette==1.0.0
uvicorn==0.43.0
uvloop==0.22.1
httptools==0.7.1
watchfiles==1.1.1
websockets==16.0
h11==0.16.0
anyio==4.13.0

# Validazione e modelli
pydantic==2.12.5
pydantic_core==2.41.5
pydantic-settings==2.14.0
annotated-types==0.7.0
annotated-doc==0.0.4
typing_extensions==4.15.0
typing-inspection==0.4.2

# Database
SQLAlchemy==2.0.49

# Autenticazione: JWT + bcrypt
python-jose==3.5.0
ecdsa==0.19.2
rsa==4.9.1
pyasn1==0.6.3
passlib==1.7.4
bcrypt==4.3.0
cryptography==46.0.6
cffi==2.0.0
pycparser==3.0

# Email validation per i campi EmailStr di pydantic
email-validator==2.3.0
dnspython==2.8.0
idna==3.11

# Upload multipart (foto/video) e immagini
python-multipart==0.0.27
pillow==12.2.0

# Config
PyYAML==6.0.3
python-dotenv==1.2.2

# Utility
click==8.3.2
six==1.17.0
